<footer class="footer">
  <div class="container footer-grid">
    <div>
      <p class="footer-logo">PizzaMania</p>
      <p class="footer-tag">Wood-fired since forever. Pineapple-tolerant since 2024.</p>
    </div>
    <div>
      <h2 class="footer-heading">Hours</h2>
      <ul class="footer-list" role="list">
        <li>Tue–Sun: 12:00–15:00, 18:00–23:30</li>
        <li>Monday: closed (dough is resting, so is Giovanni)</li>
      </ul>
    </div>
    <div>
      <h2 class="footer-heading">Find us</h2>
      <address class="footer-address">
        Vico della Mozzarella 17<br />
        80134 Napoli, Italia<br />
        <a href="tel:+390810000000">+39 081 000 0000</a>
      </address>
    </div>
    <div>
      <h2 class="footer-heading">Follow</h2>
      <ul class="footer-list" role="list">
        <li><a href="#top">Instagram</a></li>
        <li><a href="#top">Facebook</a></li>
        <li><a href="#top">TikTok</a></li>
      </ul>
    </div>
  </div>
  <div class="container footer-fine">
    <p>&copy; 2026 Pizza Mania. A fictional pizzeria for the dev.to Perfect Landing challenge. No pizzas were harmed.</p>
  </div>
</footer>

<style>
  .footer {
    background: var(--ink);
    color: var(--paper);
    border-top: var(--border);
    padding-block: var(--space-5) var(--space-4);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(13rem, 100%), 1fr));
    gap: var(--space-4);
    margin-bottom: var(--space-4);
  }

  .footer-logo {
    font-family: var(--font-display);
    text-transform: uppercase;
    font-size: var(--text-xl);
    margin-bottom: var(--space-1);
  }

  .footer-tag {
    font-family: var(--font-serif);
    font-style: italic;
    color: var(--mustard);
    font-size: var(--text-base);
  }

  .footer-heading {
    font-family: var(--font-display);
    font-size: var(--text-base);
    letter-spacing: 0.08em;
    color: var(--mustard);
    margin-bottom: var(--space-2);
  }

  .footer-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-1);
  }

  .footer-address {
    font-style: normal;
  }

  .footer a {
    color: var(--paper);
    text-underline-offset: 3px;
  }
  .footer a:hover {
    color: var(--mustard);
  }

  .footer-fine {
    border-top: 1px solid var(--ink-soft);
    padding-top: var(--space-3);
    font-size: var(--text-sm);
    color: #beb4a5;
  }
  .footer-fine p {
    margin: 0;
  }
</style>
